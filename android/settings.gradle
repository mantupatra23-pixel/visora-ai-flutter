// ✅ Cloud-safe Flutter Gradle Setup for Visora AI

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }

    // 🧠 Add Flutter SDK Build Path
    def flutterSdkPath = System.getenv("FLUTTER_HOME")
    if (flutterSdkPath == null || flutterSdkPath.isEmpty()) {
        flutterSdkPath = "${rootDir.getAbsoluteFile().getParentFile().path}/flutter"
    }
    includeBuild(flutterSdkPath)
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "visora_ai_flutter"
include(":app")

// ✅ Check flutter plugin dependencies
def flutterPluginsFile = new File(rootDir, "../.flutter-plugins-dependencies")
if (flutterPluginsFile.exists()) {
    println("✅ Flutter plugin dependencies file found at: ${flutterPluginsFile.absolutePath}")
} else {
    println("⚠️ Flutter plugin dependencies not found — will be generated by Flutter.")
}
