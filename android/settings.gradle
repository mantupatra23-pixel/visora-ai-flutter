pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "visora_ai_flutter"
include(":app")

// ✅ DO NOT LOAD JSON AS SCRIPT
def flutterPluginsFile = new File(rootDir, "../.flutter-plugins-dependencies")
if (flutterPluginsFile.exists()) {
    println("✅ Flutter plugin dependencies file found.")
} else {
    println("⚠️ Flutter plugin dependencies file not found. It will be auto-generated by Flutter.")
}
