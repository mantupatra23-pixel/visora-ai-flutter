// ✅ Cloud-safe Flutter + Gradle setup

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "visora_ai_flutter"
include(":app")

// ✅ Flutter plugin sync (for cloud build)
def flutterRoot = rootDir.getAbsoluteFile().getParentFile()
def flutterPluginsFile = new File(flutterRoot, ".flutter-plugins-dependencies")
if (flutterPluginsFile.exists()) {
    println("✅ Flutter plugin dependencies file found at: ${flutterPluginsFile.absolutePath}")
} else {
    println("⚠️ Flutter plugin dependencies file not found, generating...")
}
